{
  "name": "blog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "rm -rf _site && npx @11ty/eleventy --serve --quiet & npx tailwindcss -i ./src/input.css -o ./_site/style.css --watch",
    "build": "eleventy & npx tailwindcss -i ./src/input.css -o ./_site/style.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "tailwindcss": "^3.4.1"
  },
  "dependencies": {
    "date-fns": "^3.2.0",
    "dotenv": "^16.3.1",
    "eleventy-plugin-toc": "^1.1.5",
    "markdown-it-anchor": "^8.6.7"
  }
}
